import SwiftUI

struct PersonalizationQuizView: View {
    var body: some View {
        VStack {
            Text("Personalization Quiz")
                .font(.title)
        }
    }
}

#Preview {
    PersonalizationQuizView()
}
